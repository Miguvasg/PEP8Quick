var_one_to_send_to_function = 2
var_two_to_send_to_function = 3
var_three_to_send_to_function = 5

# las líneas se rompen antes de cada operador, y se alínean al inicio del primer argumento con el 
# objetivo de distinguir cada operador, en este caso, si se suma o se resta el siguiente argumento
result = (var_one_to_send_to_function
          + var_two_to_send_to_function
          - var_three_to_send_to_function)